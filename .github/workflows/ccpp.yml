name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: windows
    
    steps:
    - uses: actions/checkout@v2
    - name: configure
      run: mkdir build && cd build && cmake .. && MSbuild Application.sln
